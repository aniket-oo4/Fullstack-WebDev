
// it concatnates multiple strings as single string
// it does not overwrites the current string it returns newone

      var a = "Microsoft";
      var b = "Windows";
      //b.concat(a); // single string as argu 
      var c = a.concat(b,"hello "," kay challay ");// multiple strings 
      console.log(a);// not changed it is as it is 
      console.log(c);
 