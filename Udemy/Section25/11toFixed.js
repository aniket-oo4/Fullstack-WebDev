//toFixed
      // it conveerts number to string 
      // it also specifies how many decimal we are going to take
      // var a = 10;
      var a = 10.535445;
      var b = a.toFixed(3);// only takes 3 decimals
      console.log("b :",b);
      console.log(typeof b);//string
 