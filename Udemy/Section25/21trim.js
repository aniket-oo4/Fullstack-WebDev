
      // it removes the spaces from string not in middle but from left most and right mosts 
      // it does not overwrites the current string but returns new ass trimed 
      var a = "                Welcome to New Delhi                    ";
      var b = a.trim();
      console.log(a);
      console.log(b);
      console.log(a.length);
      console.log(b.length);
    